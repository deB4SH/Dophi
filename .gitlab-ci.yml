image: docker:stable

variables:
  DOCKER_HOST: tcp://docker:2375/
  DOCKER_DRIVER: overlay2

build:
  tags:
    - privileged
  services:
    - docker:dind
  before_script:
    - docker info
  image: krishofmans/gitlab-dind-maven:latest
  script:
    - mvn clean install
    - mvn deploy
